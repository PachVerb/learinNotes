#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_popper@1.0.1@popper/cli" "$@"
  ret=$?
else 
  node  "$basedir/../_popper@1.0.1@popper/cli" "$@"
  ret=$?
fi
exit $ret
