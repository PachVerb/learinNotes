#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_mocha@6.2.1@mocha/bin/mocha" "$@"
  ret=$?
else 
  node  "$basedir/../../../_mocha@6.2.1@mocha/bin/mocha" "$@"
  ret=$?
fi
exit $ret
